# MySQL - CredFlow (produção) - conexão externa
DATABASE_URL="mysql://credflow:credflow@127.0.0.1:3306/credflow"

# Só para o script de deploy criar o banco (CREATE DATABASE). User root.
MYSQL_ROOT_PASSWORD=credflow
