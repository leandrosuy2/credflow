# MySQL - CredFlow (produção)
# Na VPS use a URL INTERNA (rentflow_credflow) - o MySQL não aceita conexão pelo IP externo.
# URL interna: mesmo host/rede do painel (Credenciais).
DATABASE_URL="mysql://credflow:credflow@rentflow_credflow:3306/credflow"

# Só para o script de deploy criar o banco (CREATE DATABASE). User root.
MYSQL_ROOT_PASSWORD=credflow
